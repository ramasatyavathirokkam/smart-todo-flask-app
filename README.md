# smart-todo-flask-app
A full-stack task management web application built using Flask, implementing CRUD operations, dynamic template and persistance data storage using JSON. Designed with a responsive UI and enhanced user experience through priority and deadline tracking features.
